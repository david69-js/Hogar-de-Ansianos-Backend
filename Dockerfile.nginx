FROM nginx:alpine

# Copy the nginx configuration
COPY nginx-production.conf /etc/nginx/conf.d/default.conf

# Create the application directory (for volume mounting if needed)
RUN mkdir -p /var/www/sorherminia

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
