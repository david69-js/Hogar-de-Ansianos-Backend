services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app
    container_name: laravel-sorherminia-app
    restart: unless-stopped
    working_dir: /var/www/sorherminia
    volumes:
      - ./laravel-app:/var/www/sorherminia
    environment:
      # Laravel specific environment variables
      - APP_ENV=production
      - APP_DEBUG=false
      - LOG_CHANNEL=stderr
      # Database configuration
      - DB_CONNECTION=mysql
      - DB_HOST=mysql_sorherminia
      - DB_PORT=3306
      - DB_DATABASE=${MYSQL_DATABASE:-sorherminia}
      - DB_USERNAME=${MYSQL_USER:-sorherminia_user}
      - DB_PASSWORD=${MYSQL_PASSWORD:-root}
    networks:
      - laravel
    depends_on:
      - db
  webserver:
    image: nginx:alpine
    container_name: nginx_sorherminia
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./laravel-app:/var/www/sorherminia
      - ./nginx-production.conf:/etc/nginx/conf.d/default.conf # Configuración CON SSL
      - ./ssl:/etc/nginx/ssl # Volumen de certificados
    networks:
      - laravel
    depends_on:
      - app

  db:
    image: mysql:8.0
    container_name: mysql_sorherminia
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-sorherminia}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootsorherminia}
      MYSQL_USER: ${MYSQL_USER:-sorherminia_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-root}
    ports:
      - "${DB_PORT:-3306}:3306"
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - laravel
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      timeout: 20s
      retries: 10

networks:
  laravel:
    driver: bridge

volumes:
  dbdata:
